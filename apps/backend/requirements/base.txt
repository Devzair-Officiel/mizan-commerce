# Versions épinglées — reproductibilité des builds en dev/CI/prod.
# Pour upgrader : bumper ici, lancer la suite de tests, vérifier le changelog.

# Framework principal
Django==6.0.6
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0

# Base de données
psycopg2-binary==2.9.12

# Configuration & secrets
python-dotenv==1.2.2

# Tâches asynchrones & cache
celery==5.6.3
redis==8.0.0

# Stockage S3 (médias privés OVH)
django-storages[s3]==1.14.6
boto3==1.43.22

# Génération PDF (factures)
reportlab==4.5.1

# Sécurité — rate-limit & blocage des tentatives de login
django-axes==7.1.0

# Client HTTP moderne (multipart pour le service IA — voir apps/ocr/ai_client.py).
# `urllib.request` reste utilisé pour les sondes GET simples ; httpx s'ajoute
# uniquement quand on doit POSTer un fichier binaire avec un timeout net.
httpx==0.28.1
